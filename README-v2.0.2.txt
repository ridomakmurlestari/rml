RML Sales Visit v2.0.2
- Manual Refresh now refreshes Promo immediately using the existing text-only RPC.
- Customer sync now replaces local data even when remote returns an empty list, so deleted/deactivated outlets disappear on other devices.
- No new SQL and no new database query.
- App version 2.0.2.
